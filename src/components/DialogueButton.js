import React, { Component } from 'react';

// Import component styles
import '../appStyles.css';

class DialogueButton extends Component {
    render() {
        return (
            <div id="dialogueButton">
                Dialogue Button
            </div>
        );
    }
}

export default DialogueButton;