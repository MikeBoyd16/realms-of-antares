import React, { Component } from 'react';

// Import components
import DialogueButton from './DialogueButton';

// Import DialogueBar component styles
import '../appStyles.css';

class DialogueBar extends Component {
    render() {
        return (
            <div id="dialogueBar">
                Dialogue Bar.
            </div>
        );
    }
}

export default DialogueBar;